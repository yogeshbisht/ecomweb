export type HeaderIconType = {
  icon: ReactNode;
  label: string;
  href: string;
};

export type Highlight = {
  id: number;
  title: string;
  description: string;
  link: string;
  imgUrl: string;
};

export type Category = {
  id: string;
  title: string;
  imgUrl: string;
};
